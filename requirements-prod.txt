# Production requirements — only what the baseline model + API actually needs
# (TensorFlow, Spacy, BiLSTM etc. excluded — not used by deployed baseline model)

# API Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-multipart==0.0.6

# NLP / ML (baseline model only)
scikit-learn==1.4.0
numpy==1.26.3
python-dateutil==2.8.2

# PDF Processing
PyMuPDF==1.23.21
Pillow==10.2.0

# Utilities
python-dotenv==1.0.0
